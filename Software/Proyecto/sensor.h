#ifndef SENSOR_H
#define SENSOR_H
#include <nodo.h>

class Sensor : public Nodo
{
public:
    Sensor(); /** Constructor por defecto de la clase*/
    ~Sensor(); /** Destructor por defecto de la clase */

protected:
    // sensTyp _tipo;
};

#endif // SENSOR_H
